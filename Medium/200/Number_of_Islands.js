/**
 * Number of Islands
 */
var numIslands = function(grid) {
    let R=grid.length, C=grid[0].length;

    for (let r = 0; r < R; r++) {
        for (let c = 0; c < C; c++) {
            if(grid[r][c]=='0')continue;
            
        }
        
    }
};